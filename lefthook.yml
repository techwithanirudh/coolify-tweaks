pre-commit:
  commands:
    format:
      run: pnpm prettier --write --ignore-unknown {staged_files}
      stage_fixed: true

    build:
      run: pnpm build
      stage_fixed: true

commit-msg:
  commands:
    'lint commit message':
      run: pnpm commitlint --edit {1}
