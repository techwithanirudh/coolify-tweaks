{
  "name": "@repo/style",
  "private": true,
  "type": "module",
  "main": "src/main.scss",
  "version": "4.0.9",
  "scripts": {
    "build": "pnpm with-env bun scripts/build.ts",
    "build:watch": "pnpm with-env bun scripts/watch.ts",
    "clean": "git clean -xdf .cache .next .turbo node_modules",
    "preview": "serve dist --listen 3000 --single --no-request-logging --no-clipboard",
    "dev": "pnpm run build:watch",
    "format": "prettier --check . --ignore-path ../../.gitignore",
    "lint": "eslint --flag unstable_native_nodejs_ts_config",
    "typecheck": "tsc --noEmit",
    "check:spelling": "cspell -c .cspell.json --no-progress --no-summary --no-must-find-files --cache --cache-location .cache/.cspellcache --unique .",
    "with-env": "dotenv -e ../../.env --"
  },
  "peerDependencies": {
    "typescript": "catalog:"
  },
  "dependencies": {
    "sass-embedded": "^1.99.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@repo/cspell-config": "workspace:*",
    "@repo/eslint-config": "workspace:*",
    "@repo/prettier-config": "workspace:*",
    "@repo/tsconfig": "workspace:*",
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "@types/serve-handler": "^6.1.4",
    "browserslist": "^4.28.2",
    "commander": "^14.0.3",
    "cspell": "catalog:",
    "eslint": "catalog:",
    "lightningcss": "^1.32.0",
    "ora": "^9.4.0",
    "postcss": "^8.5.14",
    "postcss-cli": "^11.0.1",
    "postcss-load-config": "^6.0.1",
    "prettier": "catalog:",
    "serve": "^14.2.6",
    "serve-handler": "^6.1.7"
  },
  "prettier": "@repo/prettier-config"
}
